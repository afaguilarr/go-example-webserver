[pytest]
markers =
    UI: marks tests as UI, select with '-m UI', deselect with '-m "not UI"'
    API: marks tests as API, select with '-m API', deselect with '-m "not API"'

addopts = -vs -rf --html-report=./report
